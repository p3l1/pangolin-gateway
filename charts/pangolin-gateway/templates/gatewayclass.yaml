{{- if .Values.gatewayClass.create }}
apiVersion: gateway.networking.k8s.io/v1
kind: GatewayClass
metadata:
  name: {{ .Values.gatewayClass.name }}
  labels:
    {{- include "pangolin-gateway.labels" . | nindent 4 }}
spec:
  controllerName: p3l1.de/pangolin
{{- end }}
